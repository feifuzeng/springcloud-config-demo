server:
  port: 8769
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/feifuzeng/SpringcloudConfig
          search-paths: respo
          username:
          password:
      label: master
  application:
    name: config-server


